---
all:
  hosts:
    localhost:
      ansible_connection: local

      # Cisco CIMC access
      cimc_host: {YOUR_IP}
      cimc_username: admin
      cimc_password: {YOUR_PASSWORD}
      cimc_verify_tls: false

      # SSH CLI access to CIMC
      cimc_ssh_host: {YOUR_IP}
      cimc_ssh_port: 22
